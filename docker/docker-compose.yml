version: '3'
services:
  blue:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: blue-container
    ports:
      - "8081:8080"

  green:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: green-container
    ports:
      - "8082:8080"
